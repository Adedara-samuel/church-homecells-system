{
  "name": "church-homecell-system",
  "version": "1.0.0",
  "private": true,
  "description": "Church Homecell Management System - monorepo (Express/Mongo backend + Next.js frontend)",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm --workspace backend run dev",
    "dev:web": "npm --workspace frontend run dev",
    "build": "npm --workspace backend run build && npm --workspace frontend run build",
    "start": "concurrently -n api,web -c blue,magenta \"npm --workspace backend run start\" \"npm --workspace frontend run start\"",
    "seed": "npm --workspace backend run seed",
    "seed:fresh": "npm --workspace backend run seed:fresh",
    "lint": "npm --workspace backend run lint && npm --workspace frontend run lint",
    "test": "npm --workspace backend run test",
    "typecheck": "npm --workspace backend run typecheck && npm --workspace frontend run typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
